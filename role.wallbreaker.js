config = require("config");

module.exports = {
    run: function(creep) {
        // creep.pos.findClosestByRange(FIND_STRUCTURES, 1,{ filter: (structure) => (structure.structureType == STRUCTURE_WALL) }); {  console.log(creep.destroy()); }
    }
}